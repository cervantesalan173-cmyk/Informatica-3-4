#include <stdio.h>
int main(void){
    int number; //i fixed this by removing the exclamation mark
int test_value; //created space with an underscore
int example1; //number 1 was first but that cant be so i passed it to the front
float alan; //fixed the name because its not "int"
printf("Variables created successfully.\n");
}
